package mx.com.mymoduleapp.utls

/**
 * Enum que representa las categorías disponibles de películas que se pueden cargar.
 */
enum class MovieCategory {
    /** Películas actualmente en cartelera. */
    NOW_PLAYING,

    /** Películas populares. */
    POPULAR
}